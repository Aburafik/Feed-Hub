import 'package:flutter/material.dart';

class NgOsHomeVC extends StatelessWidget {
  const NgOsHomeVC({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.blue,
      ),
    );
  }
}
